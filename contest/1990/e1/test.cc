#include <bits/stdc++.h>

using namespace std;

// struct Parent : vector<array<int, 2>> {
//   Parent(Graph g, int s = 0) : vector<array<int, 2>>(g.size()) {
//     dfs(g, s, s, 1);
//   }
//   void dfs(Graph &g, int u, int p, int d) {
//     (*this)[u] = {p, d};
//     for (auto &v : g[u]) {
//       if (v != p) {
//         dfs(g, v, u, d + 1);
//       }
//     }
//   }
// };

void solve(int t) {
  // Parent par(g, 1);
  // int mole = 1;
  // auto q = [&](int x) {
  //   p('?', x);
  //   if (x == 1) {
  //     return true;
  //   }
  //   for (int i = mole; i != 1; i = par[i][0]) {
  //     if (i == x) {
  //       return true;
  //     }
  //   }
  //   mole = par[mole][0];
  //   return false;
  // };
}