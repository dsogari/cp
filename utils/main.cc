/**
 * (c) 2024 Diego Sogari
 */
#include "io.h"

/**
 * Input redirection
 */
#ifdef ONLINE_JUDGE
#define debug
#else
#include "debug.h"
init();
#endif

/**
 * Test case function
 */
void solve(int t) {
  Int n;
  println(0);
}

/**
 * Main function
 */
int main() {
  cin.tie(nullptr)->tie(nullptr)->sync_with_stdio(false);
  Int t;
  for (int i = 1; i <= t; i++) {
    solve(i);
  }
}
